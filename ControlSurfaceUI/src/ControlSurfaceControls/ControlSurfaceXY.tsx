/*

basically a 2D slider; axes spec are similar to that of knob/numeric/slider.

 {
      type: "xy";
      label: string;
      size?: "small" | "medium" | "large";
      x: {
        symbol: string,
        min?: number,
        max?: number,
        step?: number
      },
      y: {
        symbol: string,
        min?: number,
        max?: number,
        step?: number
      } 
  }
*/
