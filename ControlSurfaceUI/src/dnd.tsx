import React from "react";
import { Container, Draggable } from "react-smooth-dnd";

export const DndContainer = Container as unknown as React.FC<any>;
export const DndDraggable = Draggable as unknown as React.FC<any>;
